{
  "name": "mytapestore-redesign",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "_note": "vite is called by file path (not the PATH shim) because the parent folder name contains ':', which the shell treats as a PATH separator and breaks `vite` lookup.",
  "scripts": {
    "dev": "node ./node_modules/vite/bin/vite.js",
    "build": "node ./node_modules/vite/bin/vite.js build",
    "preview": "node ./node_modules/vite/bin/vite.js preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
