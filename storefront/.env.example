# URL of the deployed newsletter app (the newsletter/ Next.js project on Vercel).
# The footer signup form POSTs to ${VITE_NEWSLETTER_API}/api/subscribe.
# Leave blank for same-origin; set to the newsletter app URL in production.
VITE_NEWSLETTER_API=https://your-newsletter-app.vercel.app
