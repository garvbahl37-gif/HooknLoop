{%- comment -%}
  Shop policy pages. MUST be Liquid — Shopify does not support JSON templates for
  the `policy` type ("Template type 'policy' does not support JSON templates"),
  so this renders the section directly rather than through a template settings file.
{%- endcomment -%}
{% section 'mts-policy' %}
